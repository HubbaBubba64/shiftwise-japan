# Optional on Vercel because its production/preview URL variables are used as
# fallbacks. Set this to force a custom canonical origin. Use HTTPS and no path.
NEXT_PUBLIC_SITE_URL=https://shiftwise-japan.vercel.app

# Optional Google Search Console HTML-tag verification token.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-google-site-verification-token
